import 'package:flutter/material.dart';

class Otros extends StatelessWidget {
  const Otros({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body:  Center(child:  Text('Otros')));
  }
}