/*import 'package:flutter/material.dart';
import 'package:push_notificaciones/models/ruta.dart'; // Asegúrate de tener esta importación para Ruta

class RutaDetailsScreen extends StatelessWidget {
  final Ruta ruta;

  const RutaDetailsScreen({super.key, required this.ruta});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detalles de la Ruta'),
      ),
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(
              'Registro: ${ruta.registro}',
              style: const TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
            ),
            const SizedBox(height: 20),
            Text(
              'Hora: ${ruta.hora}',
              style: const TextStyle(fontSize: 18),
            ),
            // Aquí puedes agregar más detalles según lo necesario
          ],
        ),
      ),
    );
  }
}*/
