import 'package:flutter/material.dart';

class Guias extends StatelessWidget {
  const Guias({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body:  Center(child:  Text('Guias')));
  }
}